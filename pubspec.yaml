name: unipantry
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.0.3
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  cloud_firestore: ^6.1.0
  mobile_scanner: ^7.1.3
  flutter_local_notifications: ^19.5.0
  intl: ^0.20.2
  http: ^1.6.0
  uuid: ^4.5.2
  phosphor_flutter: ^2.0.0
  timezone: ^0.10.1
  image_picker: ^1.0.7
  firebase_storage: ^13.0.4
  fl_chart: 0.66.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  
  # App Icon Generator
  flutter_launcher_icons: ^0.13.1
  
flutter:
  uses-material-design: true

  assets:
    - assets/images/login_bg.png

# =============================================
# ASSET CONFIGURATION
# =============================================

# 1. App Icon Configuration (KEPT)
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  remove_alpha_ios: true
  image_path: "source_assets/icon_foreground.png"
  adaptive_icon_background: "source_assets/icon_background.png"
  adaptive_icon_foreground: "source_assets/icon_foreground.png"

